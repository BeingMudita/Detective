# Keep shell scripts LF so they run inside Linux containers regardless of the
# host OS (Windows checkouts must not turn entrypoint.sh into CRLF).
*.sh text eol=lf
entrypoint.sh text eol=lf

# Normalise common text files
*.py text
*.ts text
*.tsx text
*.json text
*.md text
